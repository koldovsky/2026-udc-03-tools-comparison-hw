# Нотатки до AGENTS.md (Task C)

## Що додано/змінено в `app/AGENTS.md`

- Узагальнив назву інструменту та прибрав специфічні формулювання, зробивши файл крос-платформною інструкцією для будь-яких ШІ-агентів.
- Додав секцію «Allowed Commands» із чітким зазначенням команд для збірки та тестів, а також прямо прописав: «Lint: не налаштовано», щоб агенти не намагалися запускати вигадані CLI-команди лінтера.
- Додав 4 конкретні конвенції коду (pure functions, збереження публічних сигнатур, без сторонніх пакетів для рядків, жорсткі ліміти `maxLength`).
- Прописав обов'язкові інженерні guardrails (тест-орієнтованість через обов'язковий запуск `npm test`, написання регресійних тестів, покриття edge cases).

## У яких інструментах перевірено (2+)

| Інструмент | Чи підхопив `AGENTS.md` без додаткових налаштувань? | Нотатки |
|---|---|---|
| **Claude Code (CLI)** | так | Нативно зчитує файл `app/AGENTS.md` під час ініціалізації сесії. Одразу враховує обмеження коду та команд у поточному контексті проєкту. |
| **ChatGPT (Web)** | частково | Самостійно файл у веб-інтерфейсі не бачить, але після ручного додавання вмісту файлу в чат (або через кастомні GPTs/System Prompt) чітко слідує кожній інструкції. |

## Промпт верифікації

> "Опиши конвенції коду та доступні команди цього проєкту згідно з його документацією, після чого додай новий порожній утилітарний метод `reverseTruncate()` за зразком `text-utils.ts`."

## Результат

- **Claude Code:** Миттєво перерахував усі 4 конвенції та згадав, що лінтер не налаштований. Створив шаблон методу в файлі, написав для нього тест-кейс і автоматично запустив `npm test`, перевіривши зелений статус перед фіналізацією відповіді.
- **ChatGPT:** Правильно виписав структуру конвенцій із наданого тексту. Згенерував код нової функції, чітко дотримавшись правила чистої функції та збереження сигнатур. У текстовому поясненні окремо зазначив, що команду лінтера запускати не потрібно, оскільки вона відсутня у конфігурації.

## Висновок

Одного узагальненого файлу `app/AGENTS.md` цілком достатньо як крос-tool baseline для координації різних ШІ-інструментів. Він позбавляє необхідності створювати безліч специфічних файлів налаштувань (на кшталт `.cursorrules` чи `.clinerules`), якщо команди та архітектурні обмеження описані зрозумілою декларативною мовою.
