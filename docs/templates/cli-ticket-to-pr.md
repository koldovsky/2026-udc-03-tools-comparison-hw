# CLI агент: тікет → PR (Task E, bonus)

> Скопіюйте у `docs/cli-ticket-to-pr.md` і заповніть.

Тікет: `materials/task-feature-ticket.md`

## CLI-агент

<напр. Claude Code / Codex CLI / Aider / Goose>

## Команда, яку запустили

```
<точна команда CLI-агента, напр. `claude "implement materials/task-feature-ticket.md"`>
```

## Що реалізовано

<2–4 речення: що саме агент змінив/додав (напр. `capitalizeWords()` у
`app/src/text-utils.ts`, тести)>

## PR

Посилання: <URL або номер PR>

Короткий diff-summary: <напр. +N/-M рядків, які файли змінено>

## Спостереження: CLI vs IDE-агент

<1–3 речення: чим цей флоу відрізнявся від роботи з IDE-агентом (Task B) —
швидкість, контроль над кожним кроком, зручність рев'ю змін, придатність
для CI>
