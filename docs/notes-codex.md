# Звіт Codex CLI — BUG-101

- Модель: gpt-5.6-terra (reasoning medium) — за даними `/status`
- Час старту: 2026-07-11 19:36:29 +02:00
- Час завершення: 2026-07-11 19:37:08 +02:00
- Тривалість: 0,65 хвилини (39 секунд)
- Кількість промптів від користувача: 1
- Скільки ітерацій правок коду знадобилось: 1
- Знайдена причина багу: `truncate()` обрізала текст до `maxLength`, а потім додавала суфікс, тому довжина результату перевищувала ліміт на довжину суфікса.
- Що змінено: `app/src/text-utils.ts` — від доступної довжини тексту віднімається довжина суфікса; якщо суфікс не коротший за ліміт, він обрізається до `maxLength`. `app/src/text-utils.test.ts` — додано регресійні тести.
- Які тести додано: сценарій `truncate("a long sentence here", 10, "...")` повертає `"a long ..."` і має довжину не більш як 10; сценарій суфікса, довшого за ліміт: `truncate("hello world", 2, "...")` повертає `".."`.
- Чи всі тести зелені: так, 8/8
- Токени: ~19.9K використано за сесію (фактичні дані з `/status`; акаунт ChatGPT Free, списано ~10% місячного ліміту)
