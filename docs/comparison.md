# Порівняння на одній задачі (Task B)

Задача: `materials/task-bug-fix.md` (та сама задача в усіх інструментах).

| Інструмент | Час до робочого фіксу | Ітерацій/промптів | Якість (знайшов справжній баг? регресії?) | Токени/вартість                              | UX-нотатки |
|---|---|---|---|----------------------------------------------|---|
| **Claude Haiku 4.5** | ~3 хв (читання → аналіз → fix → тести) | 1+1 ітерація (перша спроба edge case тесту провалилась — потрібна правка реалізації) | ✅ Знайшов справжню кореневу причину (`slice` до `maxLength - suffix.length`); обробив edge case; 0 регресій фінально (8/8 тестів ✓) | 5k input + 3k output ~ **$0.20 (20 центи)**  | Проміжна змінна `maxTextLength` для читабельності; спершу не врахував edge case в реалізації — потребувала другого редагування `text-utils.ts`; назви тестів з дужками `(regression BUG-101)` |
| **Claude Sonnet 4.6** | ~2–3 хв (читання → аналіз → fix → тести) | 1 ітерація | ✅ Знайшов справжню кореневу причину (`input.slice(0, maxLength) + suffix` не враховує довжину суфіксу в межі); обробив edge case (`suffix.length >= maxLength`); 0 регресій (8/8 тестів ✓) | 6k input + 4k output ~ **$0.50 (50 центів)** | Перед кодом явно озвучив кореневу причину в тексті; реалізація дещо компактніша (inline-вираз замість проміжної змінної); назви тестів із прив'язкою до `BUG-101`; edge case тест акуратніший — малий `maxLength = 2` з коротким суфіксом |

## Висновок

Обидва інструменти знайшли справжню кореневу причину бага (суфікс не враховується в межі зрізу) та досягли фіналу з 0 регресій і 8/8 зеленими тестами. **Sonnet 4.6** завершив за 1 чисту ітерацію: явно озвучив причину, написав компактний код та акуратні тести з прив'язкою до BUG-101. **Haiku 4.5** потребував 2 редагувань (спочатку не обробив edge case), але результат однаково якісний. **За вартістю:** Haiku $0.20 vs Sonnet $0.50 — Haiku дешевший у 3 рази при однаковому якісному результату на простому bug-fix. **Рекомендація:** для типових bug-fix завдань з чітко описаними edge cases (як у цьому тікеті) Haiku забезпечує достатню якість за мінімальну вартість; Sonnet корисний для складних рефакторів, де надійність за 1 хід критична.
