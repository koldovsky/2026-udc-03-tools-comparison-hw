# Матриця можливостей AI-інструментів (Task A)

Task A вимагає матрицю саме для **2–3 інструментів**. Тож основну матрицю я
звузив до **трьох** інструментів, з якими працюю найактивніше, а ще два виніс у
**бонус-appendix** нижче (за тими самими критеріями) — щоб головне порівняння
лишалося читабельним і в межах завдання, але зібрані дані не пропадали.

**Основні інструменти (головна матриця):**
1. **Claude Code** (CLI-інструмент від Anthropic; хобі — Pro-підписка, робота — API-білінг через корпоративний проксі)
2. **OpenCode** (open-source CLI-агент, мій основний інструмент на роботі — підключений через корпоративний проксі до GitHub Copilot та OpenAI)
3. **Antigravity 2** (агентна desktop-платформа від Google: app + CLI + SDK; хобі, лише безкоштовні моделі)

**Бонус (appendix, ті самі критерії):**
4. **GitHub Copilot** (VS Code плагін, Free-план — хобі)
5. **Codex CLI** (CLI-інструмент на базі моделей OpenAI, GPT-5.5; робота — API-білінг через корпоративний проксі)

Усі інструменти, якими я користуюсь на роботі, підключені через внутрішній корпоративний проксі (API-gateway) — тобто оплата йде за pay-per-token тарифами компанії, а не через особисті підписки. У хобі-проєктах, навпаки, використовую особисті підписки/free-тіри напряму.

---

## Спільні можливості (Common Features)

Перш ніж перейти до унікальних фіч, варто зазначити базовий рівень можливостей, який сьогодні мають практично всі сучасні AI-агенти:
* **Базова робота з файлами:** Читання, створення та редагування коду в межах робочої папки (workspace).
* **Базовий чат-інтерфейс:** Можливість ставити питання про код і отримувати пояснення без агентного виконання дій.

---

## Матриця порівняння унікальних можливостей та практичного досвіду (основні 3 інструменти)

| Критерій | Claude Code | OpenCode | Antigravity 2 |
|---|---|---|---|
| **Вибір/якість моделі** | Родина **Claude 5**: Opus 4.8 (макс. якість/reasoning), **Sonnet 5** (дефолт у Claude Code — баланс швидкість/якість), Haiku 4.5 (швидка/дешева). | 75+ провайдерів через AI SDK/Models.dev: OpenAI (у т.ч. через наявну ChatGPT Plus/Pro підписку), GitHub Copilot (через власну підписку, авторизація по device-code flow, без окремого API-ключа), Anthropic Claude, Google Gemini, AWS Bedrock, Azure OpenAI, Groq, DeepSeek, локальні моделі (Ollama/llama.cpp/LM Studio), плюс власний курований набір **OpenCode Zen**. У щоденній роботі підключаю OpenAI та GitHub Copilot через внутрішній корпоративний проксі, а не через особисті підписки. | Gemini 3.5 Flash — основна модель для швидких агентних дій і Scheduled Tasks; для складніших задач доступні додаткові reasoning-моделі там, де це підтримується. |
| **Агентна автономність та multi-step execution** | Висока. Самостійно виконує команди, править файли, робить коміти; запитує підтвердження на потенційно небезпечні дії. | Висока. Multi-session — кілька паралельних агентів в одному проєкті; primary agents + вбудовані subagents (General/Explore/Scout) для делегування підзадач; повноцінне багатокрокове виконання команд і редагувань. | Дуже висока. Standalone desktop-застосунок (IDE більше не потрібна), мульти-агентна оркестрація з паралельними суб-агентами, Scheduled Tasks (cron-подібний запуск фонових агентів), авто-тестування в браузері. |
| **Індексація кодової бази** | Локальна індексація за запитом; git-структура + файли безпосередньо в контекст моделі. | Без окремої векторної індексації — покладається на LSP (мовні сервери підвантажуються автоматично) для навігації по символах плюс пряме читання/grep файлів у контекст моделі. | Глибока семантична індексація активного воркспейсу зі швидким пошуком та розумінням кастомних інструкцій. |
| **Підтримка rules / commands / skills / MCP** | Нативний MCP-клієнт (stdio/SSE, `claude mcp add`); проєктні правила через `CLAUDE.md`/`AGENTS.md`; кастомні slash-команди (`.claude/commands/`); **Skills** (`.claude/skills/*/SKILL.md` — інструкції, які агент сам підвантажує за потреби); hooks для детермінованої логіки поза контекстом моделі. | `AGENTS.md` у корені проєкту (і піддиректоріях) для project-specific правил; MCP-сервери через конфіг (`opencode.json`/`.opencode.json`) з гранулярним permission-контролем per-tool (аж до конкретного MCP tool); primary/subagent архітектура з перемиканням між агентами по Tab. | `AGENTS.md` як базовий контекст-файл; паралельні суб-агенти; підтримка MCP; браузерний суб-агент де-факто виконує роль вбудованого «skill» для QA; SDK для побудови власних кастомних агентів. |
| **Ціна та ліміти** | Хобі: підписка Claude Pro ($20/міс) з динамічними лімітами (conversation budget) і тимчасовим кулдауном. Робота: pay-per-token через внутрішній корпоративний проксі — без місячного ліміту в стилі Pro, вартість напряму залежить від фактичного споживання токенів. | Сам інструмент безкоштовний і open-source; оплата йде лише за підключений провайдер. У моїй роботі — через внутрішній корпоративний проксі (pay-per-token за корпоративними тарифами) для OpenAI та GitHub Copilot; альтернативно можна платити провайдеру напряму або за курований OpenCode Zen/Go. | Тарифи прив'язані до Google AI-підписки: існують два окремі плани з **однаковою назвою** "AI Ultra", але різною ціною — $99.99/міс (5× лімітів Pro) і $199.99/міс (найвищі ліміти, пріоритетний доступ, включно з Project Genie). У моєму використанні — лише безкоштовний tier із базовими моделями, без AI Ultra. |
| **Enterprise-функції (SSO/SOC2/data residency)** | SSO, SOC 2 Type II, керування командами (Team/Enterprise). Data residency доступний опосередковано через хостинг на AWS Bedrock чи Google Vertex AI з вибором регіону. | Немає власних SSO/SOC2 — успадковує enterprise-гарантії від підключеного провайдера (напр. Copilot Business/Enterprise, Azure OpenAI з вибором регіону). Сайт згадує окремі enterprise-опції для самого OpenCode, деталі варто уточнювати під конкретний контракт. | SSO (Google Workspace), IAM, SOC 2/3. Data residency керується через регіон проєкту Google Cloud/Vertex AI. Версія 2 додає окремий enterprise-шлях розгортання (Managed Agents API) для контрольованого доступу команд до агентів. |
| **Приватність / політика навчання на даних** | За замовчуванням Pro-акаунти можуть навчати модель (можна вимкнути в налаштуваннях). API/Enterprise — дані не використовуються для тренування. | За заявою розробників, OpenCode не зберігає код чи контекст на власних серверах — придатний для privacy-чутливих середовищ; фактична політика навчання на даних визначається обраним провайдером (OpenAI/Copilot/інше), а не самим OpenCode. | Залежить від налаштувань Gemini-акаунту (можна вимкнути історію активності). Через Vertex AI дані не використовуються для навчання. |
| *(бонус)* **Унікальні переваги та фічі** | **Hooks** — жорстка логіка на дії агента поза контекстом моделі (не витрачає токени, гарантує безпеку правил). | **Shareable session links** — можна поділитися посиланням на конкретну агентну сесію для дебагу чи рев'ю разом з колегами; весь інструмент повністю open source (можна форкнути й аудитувати). | **Manager view** — оркестрація кількох паралельних агентів винесена в окремий інтерфейс поза редактором коду, а не додана як допоміжна фіча поверх IDE. |

---

## Бонус (appendix): ще два інструменти — GitHub Copilot та Codex CLI

Ті самі критерії, винесені окремо, щоб основна матриця лишалась у межах Task A
(2–3 інструменти). Дані зібрані з того самого досвіду, просто поза головним
порівнянням.

| Критерій | GitHub Copilot (VS Code) | Codex CLI |
|---|---|---|
| **Вибір/якість моделі** | На Free-плані вибір моделі відсутній — доступний лише режим `Auto`, який динамічно роутить запит між моделями кількох провайдерів залежно від складності задачі. Пул моделей для Copilot Chat включає GPT-5 mini, GPT-5.3-Codex, GPT-5.4, GPT-5.4 mini, Claude Haiku 4.5, Claude Sonnet 4.6, MAI-Code-1-Flash, Raptor mini. | GPT-5.5 з налаштовуваним reasoning effort — використовую на рівні **high**. |
| **Агентна автономність та multi-step execution** | Середня. Copilot Chat переважно однокроковий; агентний режим (coding agent) дозволяє більш комплексні багатокрокові зміни. | Низька/середня. Виконує задачі по кроках у терміналі; складне багатокрокове планування слабше за Claude Code/Antigravity 2 в моєму досвіді. |
| **Індексація кодової бази** | Семантична індексація кодової бази (локально у фоні; віддалено на серверах GitHub для Enterprise-репозиторіїв). | Локальна індексація/передача окремих файлів у промпті; більше ручного керування контекстом. |
| **Підтримка rules / commands / skills / MCP** | Правила через `.github/copilot-instructions.md` та per-path `*.instructions.md`; збережені промпти (`.github/prompts/*.prompt.md`); MCP-сервери підключаються через `mcp.json` у VS Code; формального поняття «skills» немає — найближчий аналог — prompt files/extensions. | `AGENTS.md` (OpenAI підтримує ту саму конвенцію); MCP-сервери конфігуруються через `~/.codex/config.toml`. |
| **Ціна та ліміти** | Free-план (моє використання): тільки `Auto`, без вибору моделі, з базовим лімітом запитів; платний план Pro (~$10/міс) додає ліміт AI-запитів із можливим овереджем. | Робочий інструмент: pay-per-token через OpenAI API, у моєму випадку — через внутрішній корпоративний проксі, а не пряму ChatGPT-підписку; вартість залежить від фактичного споживання токенів на GPT-5.5 (high effort дорожчий за low). |
| **Enterprise-функції (SSO/SOC2/data residency)** | SSO, SAML, SOC 2, фільтрація публічного коду, policy-контроль на рівні організації. GitHub Enterprise Cloud підтримує вибір регіону зберігання даних (EU/US) для частини планів. | Немає власних enterprise-функцій у стандартній CLI-обгортці. Data residency доступна через OpenAI Enterprise або Azure OpenAI (регіон Azure), а не через звичайний API-ключ. |
| **Приватність / політика навчання на даних** | Дані з Individual/Pro можуть використовуватись для навчання (можна вимкнути). Business/Enterprise — дані не тренуються. | За замовчуванням дані через OpenAI API не використовуються для навчання (окрема політика від ChatGPT-акаунтів). |
| *(бонус)* **Унікальні переваги та фічі** | **Автодоповнення (inline completions)** — генерація коду «на льоту» під час набору, без явного промпту. | Проста, передбачувана робота з терміналу; менше «магії», простіше дебажити, що саме агент зробив. |

---

## Висновок

Для моєї повсякденної роботи найсильнішим інструментом виявився **Claude Code** — завдяки якості кодування моделей родини Claude та гнучкій системі hooks, яка додає детермінізму й безпеки в автоматизацію дій без роздування контексту. **GitHub Copilot** на Free-плані не дає вибору моделі (лише `Auto`) — це різко інша філософія за автономність порівняно з Claude Code/Antigravity 2, де я сам вирішую, якою моделлю й з якою «силою» (reasoning effort) виконати задачу; головна сила Copilot лишається в моментальному inline-автодоповненні, а не в агентних сценаріях. **Antigravity 2** зробив ставку на standalone-платформу з мульти-агентною оркестрацією та Scheduled Tasks — цікавий кандидат для комплексних рефакторів чи фонової автоматизації, хоча тепер прив'язаний до дорожчих тарифів Google AI Ultra. **Codex CLI** на GPT-5.5 з high reasoning effort у моєму досвіді відчувається менш автономним за Claude Code/Antigravity 2 в багатокрокових сценаріях. **OpenCode** — мій основний робочий інструмент з конкретної причини: на роботі Claude Code й Codex CLI теж ідуть через внутрішній корпоративний проксі (усі інструменти однаково «вміють» у проксі), але кожен з них все одно розмовляє лише зі «своєю» родиною моделей — Claude Code тільки з Claude, Codex CLI тільки з OpenAI. OpenCode, сконфігурований на той самий проксі, одним інтерфейсом покриває і OpenAI, і GitHub Copilot, тож економить не на самому проксі, а на кількості окремих вендор-специфічних CLI, які інакше довелося б тримати паралельно.

**Найсуттєвіша відмінність для мого випадку:** (1) консолідація навколо одного gateway — на роботі буквально все, включно з Claude Code й Codex CLI, вже рахується як pay-per-token через внутрішній корпоративний проксі, тож різниця не в тому, чи «вміє» інструмент у проксі (вміють усі), а в тому, що OpenCode одним інтерфейсом покриває і OpenAI, і GitHub Copilot, тоді як Claude Code/Codex CLI лишаються прив'язаними кожен до своєї моделі; (2) контроль над вибором моделі — Claude Code/Codex CLI/OpenCode дають явний вибір моделі й reasoning effort, тоді як мій Copilot Free звужений до непрозорого `Auto`; (3) глибина агентної автономності (Claude Code/Antigravity 2/OpenCode vs Copilot/Codex CLI); (4) наявність структурованого способу «навчити» інструмент конвенціям проєкту (Skills/hooks у Claude Code, AGENTS.md всюди) — це прямо впливає на те, скільки разів доводиться повторювати одні й ті самі інструкції.
