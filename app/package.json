{
  "name": "udc-ws03-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tiny TS sample — common task base for the WS3 tool comparison exercise.",
  "scripts": {
    "pretest": "node -e \"require('fs').existsSync('node_modules/vitest')||require('child_process').execSync('npm install',{stdio:'inherit'})\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
